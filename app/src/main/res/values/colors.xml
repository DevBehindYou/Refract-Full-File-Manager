<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Deliberately minimal placeholders — Phase 1 has no design system (see
    architecture/MODULES.md's `core.designsystem` and the `NoHardcodedDp` lint rule's
    sibling concern for colour tokens). These two values exist only to give the window
    shell and splash screen a valid, non-null background before Compose theming exists.
    -->
    <color name="window_background_light">#FFFFFFFF</color>
    <color name="window_background_dark">#FF121212</color>
</resources>
